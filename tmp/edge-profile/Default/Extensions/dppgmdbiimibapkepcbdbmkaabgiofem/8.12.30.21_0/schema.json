{
	"type": "object",
	"properties": {
		"CredentialIntelligence": {
			"title": "Credential Intelligence settings.",
			"description": "If configured, it turns on the Credential Intelligence feature of the 1Password extension.",
			"type": "object",
			"properties": {
				"userEmail": { "type": "string" },
				"publicKey": { "type": "string" },
				"domains": { "type": "array", "items": { "type": "string" } },
				"enrollmentToken": { "type": "string" }
			},
			"required": ["userEmail", "publicKey", "domains", "enrollmentToken"]
		}
	}
}
